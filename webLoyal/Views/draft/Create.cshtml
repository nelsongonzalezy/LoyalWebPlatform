 <div class="container">
    
    <div class="d-md-flex align-items-center justify-content-between mb-4">
        <div>
            <ol class="breadcrumb fs-sm mb-1">
                <li class="breadcrumb-item"><a href="../menu/index">@webLoyal.Resources.language.Resources.Menu</a></li>
                <li class="breadcrumb-item"><a href="../Claims/index">draft</a></li>
                <li class="breadcrumb-item active" aria-current="page">@webLoyal.Resources.language.Resources.Create</li>
            </ol>
          
        </div>
    </div>
       
    <form class="row g-3 col-12">

    <div class="form-check form-switch">
     <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDefault">
     <label class="form-check-label" for="flexSwitchCheckDefault">Si desea asociar un agente seleccione</label>
    </div>

    <select id="listaNombres" disabled>
    <option value="nombre1">Agente 1</option>
    <option value="nombre2">Agente 2</option>
    <option value="nombre3">Agente 3</option>
    </select>

    <script>
    const checkbox = document.getElementById('flexSwitchCheckDefault');
    const listaNombres = document.getElementById('listaNombres');

    checkbox.addEventListener('change', function() {
    listaNombres.disabled = !this.checked;
     });
    </script>

            <form id="CreateProvider" class="row g-3 col-12" asp-action="@nameof(ProviderController.Create)" data-parsley-validate asp-controller="Provider" method="post">
            <div class="col-xl-6 col-md-12" >
                <div class="card card-one">
                    <div class="card-body row">

                        <div class="col-12">
                            <label class="form-label">@webLoyal.Resources.language.Resources.CompanyNameOfTheProvider</label>
                            <input asp-for="VendorPartnerReason" type="Text" id="@nameof(Model.VendorPartnerReason)" class="form-control"></input>
                            <span asp-validation-for="VendorPartnerReason" class="text-danger"></span>
                        </div>
                        <div class="col-12">
                            <label class="form-label">@webLoyal.Resources.language.Resources.EffectiveStartDate</label>
                            <input asp-for="@Model.EffectiveDate" type="date" class="form-control"></input>
                            <span asp-validation-for="@Model.EffectiveDate" class="text-danger"></span>

                        </div>
                    </div>
                 </div>
            </div>
                
            </form>

    de aqui para abajo es el boton de aceptar

                    <div class="col-12">
                        <button id="submitButton2" type="submit" class="btn btn-primary">@webLoyal.Resources.language.Resources.Accept</button>
                    </div>

    </form>
 </div>